fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
httpx>=0.27
python-multipart>=0.0.9
ddgs>=6.0
pypdf>=4.0
python-docx>=1.1
fpdf2>=2.7
arabic-reshaper>=3.0
python-bidi>=0.4
openpyxl>=3.1
pillow>=10.0
statsmodels>=0.14
scikit-learn>=1.3
matplotlib>=3.7
factor-analyzer>=0.5
pyreadstat>=1.2
python-pptx>=0.6
semopy>=2.3
arch>=6.3
linearmodels>=6.0
# اختیاری — برای ساخت PDF با ظاهر دقیقاً مثل Word:
#   یا LibreOffice روی سرور نصب باشد (soffice) — بهترین گزینه؛
#   یا روی ویندوز/مک با Word نصب‌شده، بسته‌ی زیر را نصب کنید:
# docx2pdf>=0.1.8

# ── «کتابخانه‌ی هوشمند» (کتاب‌های تلگرام + جستجوی معنایی) ──
# وابستگی‌هایش عمداً اینجا نیستند، چون سنگین‌اند و روی سرور کم‌رم سایت را می‌خوابانند.
# در فایل جداگانه‌ی requirements-library.txt هستند؛ برای روشن‌کردنشان README را ببین.
