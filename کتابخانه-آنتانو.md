# 📚 کتابخانه‌ی دائمی آنتانو

کتاب‌ها، جزوه‌ها، مقاله‌ها و پروپوزال‌هایی که اینجا ذخیره می‌شوند، **حافظه‌ی همیشگی**
آنتانو هستند: هنگام پاسخ‌دادن به کاربر و هنگام مقاله‌نویسی، آنتانو خودش بخش‌های مرتبط را
پیدا می‌کند و به کار می‌برد.

## کجا ذخیره می‌شود؟

فایل‌های فشرده در `data/knowledge/` — یعنی **همراه خودِ پروژه**. با هر به‌روزرسانی سرور
منتقل می‌شوند و هیچ‌وقت گم نمی‌شوند. هنگام بالا آمدن آنتانو، خودکار در پایگاه داده
بارگذاری می‌شوند (اگر قبلاً بارگذاری شده باشند، دوباره‌کاری نمی‌کند).

جستجو با موتور FTS5 خودِ SQLite انجام می‌شود — سریع، بدون سرویس بیرونی و بدون هزینه.

## افزودن کتاب یا جزوه‌ی تازه

```bash
# یک پوشه‌ی کامل
python tools/ingest_library.py ~/my-books/

# یا چند فایل مشخص
python tools/ingest_library.py book.pdf notes.docx slides.pptx
```

بعدش آنتانو را ری‌استارت کن:

```bash
docker compose up -d --build
```

نتیجه را در **پنل مدیریت → 📚 کتابخانه‌ی دائمی آنتانو** ببین.

## PDFهای اسکن‌شده

بعضی PDFها فقط عکسِ صفحه‌اند و لایه‌ی متنی ندارند. این‌ها بدون OCR قابل استفاده نیستند و
ابزار با پیام «اسکن‌شده است — با `--ocr` اجرا کن» ردشان می‌کند.

```bash
python tools/ingest_library.py ~/my-books/ --ocr
```

نکته‌ها:
- OCR **کند** است: برای هر صفحه چند ثانیه. یک کتاب ۵۰۰ صفحه‌ای ممکن است یکی‌دو ساعت
  طول بکشد. بهتر است با `screen` یا `tmux` اجرایش کنی تا با قطع‌شدن اتصال متوقف نشود.
- ابزارهای لازم داخل ایمیج داکر نصب‌اند (`tesseract-ocr`، `tesseract-ocr-fas`،
  `poppler-utils`). اگر بیرون از داکر اجرا می‌کنی:
  ```bash
  apt-get install -y tesseract-ocr tesseract-ocr-fas poppler-utils
  pip install pypdf python-docx python-pptx
  ```
- اجرای OCR داخل کانتینر:
  ```bash
  docker compose exec antanu python tools/ingest_library.py /path/to/books --ocr
  ```
  دقت کن فایل‌های تولیدشده در `data/knowledge/` باید به مخزن گیت اضافه شوند تا در
  ساخت بعدی هم بمانند.

## کیفیت متن

ابزار خودش سندهای بی‌کیفیت را کنار می‌گذارد:

- **واژه‌های به‌هم‌چسبیده**: بعضی PDFها متن را بدون فاصله بیرون می‌دهند
  (`مالیاتهایمستقیم`). چنین متنی برای جستجو بی‌فایده است و رد می‌شود — با `--ocr`
  دوباره امتحان کن.
- **قطعه‌های زباله**: بخش‌هایی مثل `= 22 )()( ~~ jjt XX XXXX` که از فرمول‌های ریاضی و
  اسکن‌های بد می‌آید، حذف می‌شوند تا بودجه‌ی پرامپت هدر نرود و مدل گیج نشود.
- **شکل‌های نمایشی عربی**: حرف‌هایی مثل `ﻗﺎﻧﻮن` خودکار به `قانون` تبدیل می‌شوند تا
  جستجو کار کند.

## آنتانو چطور از آن استفاده می‌کند؟

- **در چت**: برای هر پیام، مرتبط‌ترین بخش‌ها پیدا و به پرامپت اضافه می‌شوند، با این
  دستور که «اگر مربوط است استفاده کن و نام منبع را بیاور؛ اگر نه، نادیده بگیر».
- **در مقاله‌نویسی**: یک‌بار برای کل موضوع (هنگام طراحی فهرست بخش‌ها) و یک‌بار
  **برای هر بخش جداگانه**، تا هر بخش از نزدیک‌ترین صفحه‌های کتاب‌ها تغذیه شود.

اگر چیزی مرتبط پیدا نشود، هیچ متنی به پرامپت اضافه نمی‌شود و آنتانو مثل قبل کار می‌کند.

## حذف یک منبع

فایل `.jsonl.gz` مربوطه را از `data/knowledge/` پاک کن و آنتانو را ری‌استارت کن.
