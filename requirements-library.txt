# وابستگی‌های «کتابخانه‌ی هوشمند آنتانو» — اختیاری و جداگانه.
#
# چرا جدا؟ sentence-transformers پای PyTorch را وسط می‌کشد: حدود ۲٫۵ گیگابایت به
# ایمیج اضافه می‌کند و هنگام بردارسازی ۱ تا ۲ گیگابایت رم می‌خواهد. اگر این‌ها
# داخل requirements.txt اصلی بودند، روی یک سرور کم‌رم کل سایت از کار می‌افتاد.
#
# آنتانو بدون این‌ها کاملاً کار می‌کند؛ فقط «جستجوی معنایی در کتاب‌ها» خاموش می‌ماند
# و پنل مدیریت همین را با پیام روشن اعلام می‌کند.
#
# روشن‌کردن با داکر:
#     docker compose build --build-arg WITH_LIBRARY=1 && docker compose up -d
# روشن‌کردن بدون داکر:
#     pip install -r requirements-library.txt
#
# پیش از نصب، رم سرور را ببین:  free -h        (کمتر از ۲ گیگ ⇐ نصب نکن)

telethon>=1.36
chromadb>=0.5
sentence-transformers>=3.0
