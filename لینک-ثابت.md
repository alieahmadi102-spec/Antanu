# 🔗 لینک ثابت برای آنتانو

تانل سریع Cloudflare هر بار لینکش عوض می‌شود. دو راه برای لینک ثابت:

---

## راه ۱: ngrok با دامنه ثابت رایگان (ساده‌ترین — ۵ دقیقه)

هر اکانت رایگان ngrok یک دامنه ثابت رایگان می‌گیرد که هیچ‌وقت عوض نمی‌شود.

1. ثبت‌نام در **ngrok.com** (با جیمیل، رایگان)
2. از **dashboard.ngrok.com/get-started/setup** نسخه ویندوز را دانلود و فایل `ngrok.exe` را کنار `main.py` بگذارید
3. در همان صفحه، دستور Authtoken را کپی و در CMD اجرا کنید:
   `ngrok config add-authtoken توکن-شما`
4. از منوی **Domains** در داشبورد، روی **+ New Domain** بزنید — یک دامنه ثابت رایگان مثل `antanu.ngrok-free.app` می‌گیرید
5. فایل `tunnel-ngrok.bat` را ویرایش کنید و دامنه خودتان را بگذارید، سپس اجرا کنید

⚠️ نکته: بازدیدکننده بار اول یک صفحه هشدار ngrok می‌بیند که باید «Visit Site» را بزند (فقط یک بار در هر مرورگر). محدودیت ترافیک ماهانه هم دارد — برای شروع کافی است.

---

## راه ۲: تانل دائمی Cloudflare با دامنه خودتان (حرفه‌ای)

اگر دامنه دارید (یا یک دامنه ارزان بخرید، مثلاً .ir حدود ۱۵ هزار تومان در سال):

1. اکانت رایگان **cloudflare.com** بسازید و دامنه‌تان را اضافه کنید (NSهای دامنه را به Cloudflare تغییر دهید)
2. در CMD:
   ```
   cloudflared tunnel login
   cloudflared tunnel create antanu
   cloudflared tunnel route dns antanu chat.دامنه-شما.ir
   ```
3. فایل `tunnel-cloudflare.bat` را اجرا کنید

نتیجه: آدرس همیشگی `https://chat.دامنه-شما.ir` — بدون صفحه هشدار، بدون محدودیت، حرفه‌ای.

---

پیشنهاد: با راه ۱ شروع کنید؛ وقتی کاربران زیاد شدند به راه ۲ ارتقا دهید.
