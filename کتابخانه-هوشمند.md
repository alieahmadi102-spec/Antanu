# 📚 کتابخانه‌ی هوشمند آنتانو — کتاب‌های تلگرام + جستجوی معنایی

کتاب‌های کانال تلگرامِ خودت را می‌گیرد، متنشان را بیرون می‌کشد و طوری ذخیره می‌کند
که آنتانو بتواند بر پایه‌ی محتوای همان کتاب‌ها پاسخ بدهد.

> این با «کتابخانه‌ی دائمی» (فایل `کتابخانه-آنتانو.md`) فرق دارد و جایش را نمی‌گیرد.
> هر دو با هم کار می‌کنند:
>
> | | کتابخانه‌ی دائمی | کتابخانه‌ی هوشمند |
> |---|---|---|
> | جستجو | کلیدواژه‌ای (FTS5) | **معنایی** (بردار) |
> | منبع | فایل‌های داخل پروژه | کانال تلگرام تو |
> | نصب اضافه | ندارد | دارد (سنگین) |
>
> فرق عملی: اگر کاربر بپرسد «سود خالص چطور حساب می‌شود؟» و کتاب نوشته باشد
> «محاسبه‌ی درآمد و هزینه»، جستجوی کلیدواژه‌ای چیزی پیدا نمی‌کند ولی جستجوی
> معنایی همان بخش را می‌آورد.

---

## ⚠️ اول از همه: رم سرورت را ببین

این قابلیت سنگین است. `sentence-transformers` پای PyTorch را وسط می‌کشد:
حدود ۲٫۵ گیگابایت به ایمیج اضافه می‌کند و هنگام بردارسازی ۱ تا ۲ گیگابایت رم می‌خواهد.

روی سرور اجرا کن:

```bash
free -h
df -h
```

- **رم کمتر از ۲ گیگ** ⟵ نصب نکن. سایت می‌خوابد. راه جایگزین را پایین‌تر بخوان.
- **رم ۲ تا ۴ گیگ** ⟵ کار می‌کند، ولی هنگام همگام‌سازی سایت کند می‌شود.
  شب یا ساعت خلوت همگام‌سازی کن.
- **رم بیشتر از ۴ گیگ** ⟵ بدون نگرانی.

به فضای دیسک هم نگاه کن: دست‌کم **۵ گیگابایت آزاد** لازم است.

**تا وقتی این را نصب نکنی، آنتانو دقیقاً مثل الان کار می‌کند** — چت، مقاله‌ی بلند،
تحلیل آماری، تولید عکس، پنل مدیریت و اشتراک همه سرِ جایشان‌اند. فقط جستجوی معنایی
خاموش می‌ماند و پنل مدیریت همین را با پیام روشن می‌گوید.

---

## گام ۱: کلیدهای تلگرام را بگیر

۱. برو به **my.telegram.org** و با شماره‌ی تلفن خودت وارد شو.
۲. روی **API development tools** بزن.
۳. یک اپ بساز (اسمش هر چیزی می‌تواند باشد، مثلاً `antanu`).
۴. دو مقدار به تو می‌دهد: **api_id** و **api_hash**.

روی سرور، در فایل `.env` این‌ها را بگذار:

```bash
nano .env
```

```
TELEGRAM_API_ID=1234567
TELEGRAM_API_HASH=0123456789abcdef0123456789abcdef
TELEGRAM_BOOKS_CHANNEL=@my_books_channel
```

نکته‌ها:
- `TELEGRAM_BOOKS_CHANNEL` می‌تواند نام کاربری کانال (`@my_books`) یا آیدی عددی‌اش
  (`-1001234567890`) باشد.
- باید **عضو آن کانال** باشی، وگرنه تلگرام اجازه‌ی خواندن نمی‌دهد.
- **چرا حساب کاربری و نه ربات؟** ربات‌های تلگرام فقط تا ۲۰ مگابایت می‌توانند فایل
  بگیرند و بیشتر کتاب‌ها بزرگ‌ترند. با حساب خودت این محدودیت نیست.

---

## گام ۲: قابلیت را روشن کن و بساز

این خط را هم به همان فایل `.env` اضافه کن:

```
WITH_LIBRARY=1
```

**چرا داخل `.env` و نه روی خط فرمان؟** چون پیش‌فرضش خاموش است. اگر فقط یک بار با
`--build-arg` بسازی، دفعه‌ی بعد که به‌روزرسانی معمولی بزنی (`docker compose up -d
--build`) دوباره خاموش می‌شود و کتابخانه بی‌صدا از کار می‌افتد. وقتی در `.env`
باشد، هر ساختِ بعدی هم روشن می‌ماند.

```bash
cd ~/Antanu
git pull
docker compose up -d --build
```

این ساخت **طولانی است** (بسته به سرعت اینترنت سرور، ۱۰ تا ۳۰ دقیقه) چون PyTorch را
دانلود می‌کند. صبور باش.

اگر بدون داکر اجرا می‌کنی:

```bash
pip install -r requirements-library.txt
```

---

## گام ۳: یک بار به تلگرام وارد شو

تلگرام برای ورود یک کد تأیید می‌فرستد که باید همان لحظه تایپ شود؛ این کار از داخل
صفحه‌ی وب شدنی نیست، پس **یک بار** در ترمینال انجام می‌شود:

```bash
docker compose run --rm -it antanu python tools/telegram_login.py
```

بدون داکر:

```bash
python tools/telegram_login.py
```

شماره‌ات را می‌پرسد، کد را به تلگرامت می‌فرستد، کد را وارد می‌کنی (اگر رمز دومرحله‌ای
داری، آن را هم می‌پرسد). در پایان می‌گوید با چه حسابی وارد شدی و آیا کانال را پیدا
کرده است.

بعد از این، فایل `antanu.session` ساخته می‌شود و **دیگر هیچ‌وقت کد نمی‌خواهد**.

> 🔒 این فایل مثل رمز عبور توست. در `.gitignore` هست و هرگز نباید به گیت‌هاب یا
> جای دیگری برود.

---

## گام ۴: کتاب‌ها را همگام کن

وارد **پنل مدیریت** شو ← کارت **«📚 کتابخانه‌ی هوشمند (کتاب‌های تلگرام)»** ←
دکمه‌ی **«🔄 همگام‌سازی کتاب‌ها»**.

پیشرفت همان‌جا نشان داده می‌شود («۱۲ از ۸۰…»). می‌توانی صفحه را ببندی؛ کار در
پس‌زمینه ادامه دارد.

بار اول کندتر است چون مدل بردارساز (حدود ۵۰۰ مگابایت) هم دانلود می‌شود.

چه اتفاقی می‌افتد:
- فقط فایل‌های `pdf، docx، txt، md، pptx، xlsx` برداشته می‌شوند.
- کتابی که قبلاً اضافه شده، **دوباره دانلود نمی‌شود**.
- PDF اسکن‌شده (عکسِ صفحه، بدون متن) خودکار رد می‌شود.
- خطای یک کتاب بقیه را متوقف نمی‌کند.
- فایل خام بعد از استخراج متن **حذف می‌شود** تا دیسک پر نشود.

بعداً که کتاب تازه‌ای به کانال اضافه کردی، دوباره همین دکمه را بزن — فقط کتاب‌های
جدید پردازش می‌شوند.

---

## گام ۵: استفاده

در چت، دکمه‌ی **«📖 جستجو در کتابخانه»** را روشن کن و بپرس. آنتانو پاسخ را از روی
کتاب‌ها می‌سازد و **نام کتاب منبع** را ذکر می‌کند. اگر در کتاب‌ها چیزی نبود، صریح
می‌گوید که پیدا نکرده و از خودش چیزی نمی‌بافد.

با دکمه‌ی خاموش هم کتاب‌ها به کار می‌روند، ولی سبک‌تر و در کنار بقیه‌ی دانش آنتانو.

متن این دکمه خودکار به زبان کاربر ترجمه می‌شود.

---

## به‌روزرسانی‌های بعدی

بعد از این‌که یک بار راه افتاد، هر به‌روزرسانی همین سه خط است:

```bash
cd ~/Antanu
git pull
docker compose up -d --build
```

نه لازم است دوباره وارد تلگرام شوی، نه دوباره کتاب‌ها را همگام کنی: نشست تلگرام و
پایگاه برداری روی دیسک ماندگار `/data` می‌مانند و ساختِ تازه به آن‌ها دست نمی‌زند.

`WITH_LIBRARY=1` هم چون در `.env` است خودکار اعمال می‌شود.

بعد از هر به‌روزرسانی، پنل مدیریت ← کارت **«🔧 نسخه و وضعیت ساخت»** را نگاه کن.
سه سطر مربوط به کتابخانه‌ی هوشمند باید ✅ باشند. اگر ❌ بودند، یعنی فایل‌های تازه
روی سرور ننشسته‌اند:

```bash
docker compose build --no-cache && docker compose up -d
```

**خاموش‌کردن دوباره‌ی کتابخانه‌ی هوشمند** (اگر دیدی سرور کم آورده): خط
`WITH_LIBRARY=1` را در `.env` کامنت کن و `docker compose up -d --build` بزن.
بقیه‌ی آنتانو کامل کار می‌کند و کتاب‌های ذخیره‌شده هم پاک نمی‌شوند — هر وقت
دوباره روشنش کردی سرِ جایشان هستند.

---

## اگر سرور کوچک بود — راه جایگزین

می‌توانی سنگینیِ کار را روی کامپیوتر شخصی خودت انجام بدهی و فقط نتیجه را بالا بفرستی:

```bash
# روی کامپیوتر خودت (ویندوز/مک/لینوکس)
git clone <آدرس پروژه> && cd Antanu
pip install -r requirements.txt -r requirements-library.txt
cp .env.example .env      # کلیدهای تلگرام را بگذار
python tools/telegram_login.py
python -c "from library import telegram_sync; print(telegram_sync.sync_channel_blocking())"
```

حالا پوشه‌ی `chroma_db/` ساخته شده. آن را روی سرور بفرست:

```bash
tar czf chroma_db.tar.gz chroma_db/
scp chroma_db.tar.gz user@your-server:/tmp/

# روی سرور
docker compose cp /tmp/chroma_db.tar.gz antanu:/data/
docker compose exec antanu tar xzf /data/chroma_db.tar.gz -C /data/
docker compose restart
```

سرور همچنان به `sentence-transformers` نیاز دارد (برای بردارسازیِ *پرسش* کاربر)،
ولی دیگر لازم نیست ده‌ها کتاب را پردازش کند — یعنی رم کمتری در لحظه می‌خواهد.

---

## عیب‌یابی

**کارت می‌گوید «تنظیم نشده»**
کلیدهای تلگرام در `.env` نیستند یا کانتینر بعد از ویرایش `.env` ری‌استارت نشده:
`docker compose restart`

**کارت می‌گوید «وارد نشده»**
گام ۳ را انجام بده (`python tools/telegram_login.py`).

**پیام «مدل بردارساز بارگذاری نشد»**
سرور به `huggingface.co` دسترسی ندارد یا رم کم آورده. اگر پشت فیلترینگ هستی،
راه جایگزین بالا را برو.

**پیام «chromadb و sentence-transformers نصب نیستند»**
با `--build-arg WITH_LIBRARY=1` نساخته‌ای. گام ۲ را دوباره ببین.

**بعد از build همه چیز پاک شد؟**
نباید بشود — پایگاه برداری و نشست تلگرام روی دیسک ماندگار `/data` می‌مانند.
اگر `docker compose down -v` زدی، آن `-v` خودِ دیسک را پاک می‌کند؛ نزن.

**بررسی سلامت بعد از هر به‌روزرسانی**
پنل مدیریت ← کارت **«🔧 نسخه و وضعیت ساخت»**. اگر کنار موردی ❌ بود، فایل‌های تازه
هنوز روی سرور ننشسته‌اند.

**اجرای آزمون‌ها روی سرور**

```bash
docker compose exec antanu python test_library.py
```

---

## مسئولیت محتوا

کانال متعلق به خودت است و مسئولیت حق‌ونشرِ کتاب‌هایی که در آن می‌گذاری با توست.
